/**
 * 1. create firebase project
 * 2. enable web
 * 3. enable sign in method
 * 4. get firebase config in firebase.config.js
 * 5.export app from <firebase className="config js"></firebase>
 */

//authentication
/**
 * 1. create userCOntext (AuthContext)
 * 2. set AuthCOntext.Provider
 * 3. Make sure you set the children
 * 4. export AuthContext to be used inside useCOntext hook
 * 5.get form data
 * 6. getAuth in the userContext
 */

/**
 * firebase hosting
 * One time for one computer
 * 1.npm install -g firebase-tools
 * 2.firebase login
 * 
 * for each project firebase init
 * make sure : for public directory  is -build.
 * single page application -yes
 * 
 * //for every deploy
 * ..>npm run build
 * ..>firebase deploy
 */


